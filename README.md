# Data-binding [R]evolution slides

### Content Authoring

Run `grunt dev` when working on any `slides/*.md` files to see changes in the respective `output/*.html` files.

### Zip Distribution

When ready to upload files, call `grunt zip` and they will be zipped up.
